#include<bits/stdc++.h>
using namespace std;

vector<int> solve(vector<int> &vec, int k) {
    for (int i = 0; i < )
}

int main() {
    vector<int> vec = {5,10,15};
    int k;
    cin >> k;
    vector<int> vec2 = solve(vec, k);
}